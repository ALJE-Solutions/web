
<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>ALJE Solutions</title>
		<style>
			:root {
				--primary: #0a192f;
				--accent: #64ffda;
				--bg: #f0f4f8;
				--radius: 12px;
				--shadow: 0 4px 24px rgba(10,25,47,0.08);
				--transition: 0.3s cubic-bezier(.4,0,.2,1);
			}
			body {
				margin: 0;
				font-family: 'Segoe UI', 'Inter', Arial, sans-serif;
				background: linear-gradient(120deg, #f0f4f8 0%, #e6f7ff 100%);
				color: var(--primary);
				min-height: 100vh;
				scroll-behavior: smooth;
			}
			nav {
				background: rgba(10,25,47,0.98);
				color: #fff;
				padding: 0.7rem 2.2rem;
				box-shadow: var(--shadow);
				position: static;
				z-index: 10;
				animation: navFadeIn 1s ease;
				margin-bottom: 0;
				display: flex;
				align-items: center;
				justify-content: space-between;
			}
			.nav-brand {
				display: flex;
				align-items: center;
				gap: 0.7rem;
			}
			.nav-brand img {
				width: 28px;
				height: 28px;
				border-radius: 50%;
				background: #fff;
				object-fit: cover;
				box-shadow: 0 2px 8px rgba(10,25,47,0.10);
			}
			.nav-brand span {
				font-size: 1.35rem;
				font-weight: 700;
				letter-spacing: 1px;
				color: #64ffda;
			}
			.nav-links {
				display: flex;
				align-items: center;
				gap: 2.2rem;
			}
			@keyframes navFadeIn {
				from { opacity: 0; transform: translateY(-30px); }
				to { opacity: 1; transform: translateY(0); }
			}
			nav a {
				color: #fff;
				text-decoration: none;
				font-weight: 500;
				letter-spacing: 1px;
				position: relative;
				transition: color var(--transition);
				font-size: 1.08rem;
				padding: 0.2rem 0.1rem;
			}
			nav a::after {
				content: '';
				display: block;
				width: 0;
				height: 2px;
				background: var(--accent);
				transition: width var(--transition);
				margin: 0 auto;
			}
			nav a:hover {
				color: var(--accent);
			}
			nav a:hover::after {
				width: 100%;
			}
			main {
				min-height: 70vh;
				animation: mainFadeIn 1.2s cubic-bezier(.4,0,.2,1);
				margin-top: 0;
			}
			@keyframes mainFadeIn {
				from { opacity: 0; transform: translateY(40px); }
				to { opacity: 1; transform: translateY(0); }
			}
			section {
				background: #fff;
				border-radius: var(--radius);
				box-shadow: var(--shadow);
				margin-bottom: 2.5rem;
				padding: 2rem 1.5rem;
				transition: box-shadow var(--transition), transform var(--transition);
				animation: sectionFadeIn 1.2s cubic-bezier(.4,0,.2,1);
			}
			@keyframes sectionFadeIn {
				from { opacity: 0; transform: translateY(60px); }
				to { opacity: 1; transform: translateY(0); }
			}
			section:hover {
				box-shadow: 0 8px 32px rgba(100,255,218,0.10);
				transform: translateY(-4px) scale(1.01);
			}
			h1, h2, h3 {
				font-family: 'Segoe UI', 'Inter', Arial, sans-serif;
				font-weight: 700;
				letter-spacing: 1px;
				margin-top: 0;
			}
			h1 {
				font-size: 2.7rem;
				color: var(--primary);
				text-shadow: 0 2px 8px rgba(10,25,47,0.08);
				animation: titlePopIn 1.1s cubic-bezier(.4,0,.2,1);
			}
			@keyframes titlePopIn {
				from { opacity: 0; transform: scale(0.95); }
				to { opacity: 1; transform: scale(1); }
			}
			h2 {
				color: var(--accent);
				font-size: 2rem;
				margin-bottom: 1rem;
			}
			ul {
				padding: 0;
				margin: 0;
			}
			ul li {
				background: #e6f7ff;
				border-radius: var(--radius);
				margin-bottom: 1.2rem;
				padding: 1.2rem 1rem;
				box-shadow: 0 2px 8px rgba(10,25,47,0.04);
				transition: box-shadow var(--transition), transform var(--transition);
				animation: listFadeIn 1.3s cubic-bezier(.4,0,.2,1);
			}
			@keyframes listFadeIn {
				from { opacity: 0; transform: translateY(30px); }
				to { opacity: 1; transform: translateY(0); }
			}
			ul li:hover {
				box-shadow: 0 6px 24px rgba(100,255,218,0.13);
				transform: translateY(-2px) scale(1.01);
			}
			a {
				transition: color var(--transition), background var(--transition);
			}
			a:focus {
				outline: 2px solid var(--accent);
				outline-offset: 2px;
			}
			footer {
				background: var(--primary);
				color: #fff;
				text-align: center;
				padding: 1rem 0;
				margin-top: 2rem;
				font-size: 0.95rem;
				letter-spacing: 1px;
				box-shadow: 0 -2px 8px rgba(10,25,47,0.04);
				animation: footerFadeIn 1.2s cubic-bezier(.4,0,.2,1);
			}
			@keyframes footerFadeIn {
				from { opacity: 0; transform: translateY(30px); }
				to { opacity: 1; transform: translateY(0); }
			}
			@media (max-width: 700px) {
				nav a { margin: 0 0.7rem; font-size: 1rem; }
				main, section { padding: 1rem 0.5rem; }
				h1 { font-size: 2rem; }
				h2 { font-size: 1.3rem; }
			}
		</style>
	</head>
	<body>
		<nav>
			<div class="nav-brand">
				<img src="/src/images/logo_sinfondo.png" alt="Logo ALJE Solutions" />
				<span>ALJE Solutions</span>
			</div>
			<div class="nav-links">
				<a href="/">Inicio</a>
				<a href="#servicios">Servicios</a>
				<a href="#contacto">Contacto</a>
			</div>
		</nav>
		<slot />
		<footer>
			© {new Date().getFullYear()} ALJE Solutions. Todos los derechos reservados.
		</footer>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
</style>
